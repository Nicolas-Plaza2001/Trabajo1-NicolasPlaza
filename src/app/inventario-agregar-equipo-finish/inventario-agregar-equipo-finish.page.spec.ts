import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InventarioAgregarEquipoFinishPage } from './inventario-agregar-equipo-finish.page';

describe('InventarioAgregarEquipoFinishPage', () => {
  let component: InventarioAgregarEquipoFinishPage;
  let fixture: ComponentFixture<InventarioAgregarEquipoFinishPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InventarioAgregarEquipoFinishPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
